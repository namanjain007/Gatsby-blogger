import React from 'react';

import Layout from '../components/layout';

const BLog = () => {
    return (
        <Layout>
            This is a template blog for markdown data!!!
        </Layout>
    );
};

export default BLog;