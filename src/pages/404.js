import React from 'react';

import Layout from '../components/layout';

const Error = () => {
    return (
        <div>
            <Layout>
            <h1>
                404...
            </h1>
            </Layout>
        </div>
    );
};

export default Error;